<%= turbo_frame_tag "joke_result" do %>
  <div class="mt-6 p-4 border rounded bg-yellow-50 shadow">
    <h2 class="text-xl font-bold mb-2">🤣 Here's your joke:</h2>

    <% if joke["type"] == "single" %>
      <p class="text-lg"><%= joke["joke"] %></p>
    <% elsif joke["type"] == "twopart" %>
      <p class="text-lg mb-2"><strong>Setup:</strong> <%= joke["setup"] %></p>
      <p class="text-lg"><strong>Punchline:</strong> <%= joke["delivery"] %></p>
    <% else %>
      <p class="text-red-600">Oops! Couldn’t understand this joke format.</p>
    <% end %>
  </div>
<% end %>
