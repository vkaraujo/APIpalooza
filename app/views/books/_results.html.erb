<%= turbo_frame_tag "books_result" do %>
  <div class="mt-8">
    <h2 class="text-2xl font-bold text-center mb-6">📖 Search Results</h2>

    <% if data.empty? %>
      <p class="text-gray-500 text-center">No books found for “<%= query %>”. Try a different keyword!</p>
    <% else %>
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
        <% data.each do |book| %>
          <div class="border rounded-lg p-4 bg-white shadow space-y-2 flex flex-col items-center text-center">
            <% if book["cover_i"] %>
              <img src="https://covers.openlibrary.org/b/id/<%= book["cover_i"] %>-M.jpg"
                   alt="Cover of <%= book["title"] %>"
                   class="w-32 h-auto rounded shadow-sm mb-2" />
            <% end %>

            <h3 class="text-md font-semibold"><%= book["title"] %></h3>

            <% if book["author_name"] %>
              <p class="text-sm text-gray-700">👤 <%= book["author_name"].join(", ") %></p>
            <% end %>

            <% if book["first_publish_year"] %>
              <p class="text-sm text-gray-600">📅 <%= book["first_publish_year"] %></p>
            <% end %>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>
<% end %>
