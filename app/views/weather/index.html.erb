<h1 class="text-3xl font-bold mb-6">🌦️ Weather Lookup</h1>

<%= form_with url: weather_index_path, method: :post, data: { turbo_frame: "weather_result" }, class: "space-y-4" do |form| %>
  <%= form.label :city, "Enter a city", class: "block font-semibold" %>
  <%= form.text_field :city, class: "border rounded px-4 py-2 w-full" %>
  <%= form.submit "Get Weather", class: "bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600" %>
<% end %>

<%= turbo_frame_tag "weather_result" do %>
  <p class="text-gray-500">Enter a city above to see the weather!</p>
<% end %>
